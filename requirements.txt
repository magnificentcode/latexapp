# Requirements for latexapp — FastAPI backend + Railway Postgres

fastapi==0.104.1
uvicorn[standard]==0.22.0
pydantic==2.5.0
email-validator==2.1.0.post1

# Database (Railway Postgres)
sqlalchemy==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# Auth
bcrypt==4.2.1
python-jose==3.3.0

python-dotenv==1.0.1

# Math rendering (LaTeX -> SVG for the rich-text editor's inline
# equations, via mathtext -- no full LaTeX install needed for previewing).
matplotlib==3.11.0
